package com.example.wiprotest.utils;import org.junit.jupiter.api.Test;import java.text.ParseException;import java.util.Date;import java.util.GregorianCalendar;import static org.junit.jupiter.api.Assertions.*;class DateParserTest {	@Test	void parseDumpDate() throws ParseException {		// GIVEN		String input = "08-Jan-1996";		// WHEN		Date parsedDate = DateParser.parseDumpDate(input);		// THEN		GregorianCalendar calendar = new GregorianCalendar();		calendar.setTime(parsedDate);		assertEquals(8, calendar.get(GregorianCalendar.DAY_OF_MONTH));		assertEquals(GregorianCalendar.JANUARY, calendar.get(GregorianCalendar.MONTH));		assertEquals(1996, calendar.get(GregorianCalendar.YEAR));	}}